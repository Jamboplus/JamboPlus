name: jamboad_server
description: JamboAd admin and app API backend (Shelf + SQLite).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.4.0
  crypto: ^3.0.6
  dart_jsonwebtoken: ^2.14.1
  uuid: ^4.5.1

dev_dependencies:
  lints: ^5.0.0
